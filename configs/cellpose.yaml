model_name: cellpose

runtime:
  device: cuda

model:
  pretrained_model: null

inference:
  channel_axis: -1
  normalize: true
  diameter: null

output:
  save_instance_masks: true
  save_flows: false
  save_styles: false