model_name: unet_watershed

runtime:
  device: cuda

model:
  checkpoint_path: outputs/checkpoints/unet_fg_biased/unet_best.pt
  in_channels: 3
  out_channels: 1
  base_channels: 32

inference:
  tile_size: 256
  threshold: 0.45
  min_size: 16
  min_distance: 9

output:
  save_instance_masks: true